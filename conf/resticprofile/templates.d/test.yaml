test:
  inherit: "default"

  initialize: true

  backup:
    # Base path for all entries in 'source'
    #source-base:

    # Backup directories
    source:
      - "{{ .ConfigDir }}/../test/data"

    iexclude:
      - "Downloads"

    # Tags
    tag:
      - "{{ .Profile.Name }}"
      - "{{ .OS }}"

    # Schedule
    # schedule: "weekly"

  check:
    # Schedule
    # schedule: "daily"
