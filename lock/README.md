# 🔒 lock/

This folder contains lock files created by `restic` and `resticprofile`.

✅ These files ensure that backup operations do not overlap or interfere with each other.

- Automatically created and removed by the tools.
- Safe to delete if no backup or prune is currently running.
- Do **not** include this folder in backups or version control.
